# step-11-gltf-scene

Code snapshot for the corresponding [lab step](../TP.md) — complete demo with Notre-Dame (glTF) and Tour Eiffel (Collada).

```bash
npm install
npm start
```

3D models are served from `public/models/` (symlinks to `data/`).
