# step-10-tour-eiffel

Code snapshot for the corresponding [lab step](../TP.md) — loading the `Tour_Eiffel.dae` model (Collada / SketchUp).

```bash
npm install
npm start
```

The model is served from `public/models/tour-eiffel/` (symlink to `data/tour_eiffel/`).
